name: cargo-stage
from: docker.io/library/rust
modules:
  - type: script
    snippets:
      - apt-get update && apt-get install -y pkg-config libssl-dev libdbus-1-dev
      - cargo install bluetui impala
