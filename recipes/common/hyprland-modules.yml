modules:
  - type: dnf
    repos:
      copr:
        enable:
          - lionheartp/Hyprland
          - scottames/ghostty
          - avengemedia/danklinux
          - avengemedia/dms
    install:
      install-weak-deps: false
      packages:
      - ghostty
      - hyprland
      - dms
      - hyprland-plugin-hyprscrolling-git
      - xdg-desktop-portal-hyprland
      - hyprpaper
      - hyprpolkitagent
      - hyprland-guiutils
      - hyprshot
      - jetbrains-mono-fonts
      - uwsm

  - type: dnf
    repos:
      copr:
        enable:
        - sdegler/hyprland
    install:
      install-weak-deps: false
      packages:
      - hyprsysteminfo

  - type: dnf
    repos:
      copr:
        enable:
          - washkinazy/wayland-wm-extras
          - erikreider/SwayNotificationCenter
    install:
      install-weak-deps: false
      packages:
      - swayosd
      - waypaper
      - gtk4-layer-shell

  - type: files
    files:
      - source: system/hyprland
        destination: /
