#!/usr/bin/env bash
# DEPRECATED: Tarion scripts are now installed in /usr/bin/
# This utility is kept for backward compatibility only

set -euo pipefail

echo "================================================"
echo "⚠️   tarion-update-scripts is DEPRECATED"
echo "================================================"
echo ""
echo "Tarion scripts are now installed directly in /usr/bin/"
echo "No manual script updates are needed."
echo ""
echo "If you have old scripts in ~/.local/bin/, you can:"
echo "  1. Remove them: rm ~/.local/bin/tarion-*"
echo "  2. Use the system scripts in /usr/bin/ instead"
echo ""
echo "All Tarion commands are now available directly:"
echo "  tarion-keybindings-show"
echo "  tarion-wallpaper-switcher"
echo "  tarion-theme-set"
echo "  ...and others"
echo ""
echo "================================================"
exit 0
