# Application launch bindings
# Variables ($terminal, $fileManager, etc.) are defined in apps/defaults.conf
bindd = SUPER, RETURN, Open terminal, exec, $terminal
bindd = SUPER SHIFT, F, File manager (Superfile), exec, $fileManager
