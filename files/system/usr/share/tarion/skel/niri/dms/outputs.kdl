// Placeholder seeded by tarion-sync. DMS manages monitor output configuration
// here at runtime (typically as a symlink to a generated profile under
// ~/.config/niri/dms/profiles/). Empty by default → niri uses autodetected modes.
